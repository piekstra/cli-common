[package]
name = "pk-cli-scrape"
description = "Minimal HTML scanning for the piekstra CLI family: read values out of server-rendered provider pages without a DOM parser dependency."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true

# No dependencies on purpose — see the crate docs. Everything here is string
# scanning over `&str`.
[dependencies]
